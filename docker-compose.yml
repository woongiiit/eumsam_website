version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./eumsaem.db
      - SECRET_KEY=eumsaem-band-secret-key-2024
    volumes:
      - ./data:/app/data
    restart: unless-stopped
